<h3> Edit the link: </h3>

<%= form_for([@user, @link]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <%= f.submit 'Update'%>
<% end %>
