<p><%= link_to @link.title, "http://#{@link.url}" %></p>

<% @comments.each do |user_name, content| %>
  <p><%= user_name %>: <%= content %></p>
<% end %>

<% if logged_in? %>
  <%= form_for([@user, @link, @link.comments.build]) do |f| %>
    <%= f.label :comment %>
    <%= f.text_area :content %>
    <%= f.submit "submit" %>
  <% end %>
<% else %>
  <p>
    <%= link_to 'log in', login_path %>, don't have account? <%= link_to 'signup', signup_path %>
  </p>
<% end %>
