<h3>Links: </h3>

<% @all_share_links.each do |user, link| %>
  <p>
    <%= link_to link.title, "http://#{link.url}" %> shared by <%= link_to user.name, user_links_path(user) %>
  </p>
  <p>
    <%= link_to 'comments', user_link_path(user, link) %>
  </p>
<% end %>
